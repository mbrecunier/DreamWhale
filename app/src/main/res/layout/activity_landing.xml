<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageView2"
        android:src="@drawable/background"
        android:scaleType="centerCrop"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="LogIn To dream"
        android:id="@+id/loginButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:width="200dp"
        android:textSize="17sp"
        android:padding="15dp"
        android:textColor="@color/colorTextIcons"
        android:background="#90446CA0"
        android:drawableLeft="@drawable/ic_filter_drama_white_24dp"
        android:drawablePadding="4dp"
        android:clickable="false" />

    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="Community dreams"
        android:id="@+id/viewPublicJournalButton"
        android:layout_below="@+id/loginButton"
        android:layout_centerHorizontal="true"
        android:width="200dp"
        android:textSize="17sp"
        android:layout_marginTop="20dp"
        android:padding="15dp"
        android:textColor="@color/colorTextIcons"
        android:background="#90673AB7"
        android:drawableLeft="@drawable/ic_people_white_24dp"
        android:drawablePadding="4dp" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:contentDescription="picture of whale"
        android:src="@drawable/pinkwhale"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_above="@+id/loginButton"
        android:scaleType="centerCrop"
        android:cropToPadding="false" />



</RelativeLayout>
